Mopedgarage – interne Release-Notizen

Die Extension ist jetzt auf einen ersten öffentlichen Release vorbereitet.

Wichtig:
- `v100` ist ab dieser Fassung eingefroren.
- Weitere Änderungen an Schema, Config oder Modulen nur noch über neue Migrationen (`v101`, `v102`, ...).
- Öffentliche Doku liegt jetzt in `README.md` und `CHANGELOG.md`.
- Screenshots vor Veröffentlichung in `screenshots/` ergänzen.
